#include <stdio.h>#include <stdlib.h>typedef struct node{    char * word;    int frequency;    struct node *next;}node_t;node_t *push(node_t *pTop, char * string);node_t *newE();int main(){    printf("Hello world!\n");    return 0;}node_t *newE(){    node_t *ePtr;    ePtr=malloc(sizeof(node_t));    if(ePtr==NULL){        fprintf(stderr,"allocation error!\n");        exit(FAILURE);    }        return (ePtr);}node_t *push(node_t *pTop, char * string){    node_t *pNew;    pNew=newE();    pNew->}}
