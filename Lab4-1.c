#include <stdio.h>#include <stdlib.h>#include <string.h>int main(){    FILE *f1,*f2;    char ** mat;    char *tmp;    int i=0,j,n;    tmp=malloc(100*sizeof(char));    f1=fopen("input.txt", "r");    fscanf(f1, "%d", &n);    mat=malloc(n*sizeof(char *));    while(fscanf(f1,"%s",tmp)!=EOF){        mat[i]=malloc(strlen(tmp)*sizeof(char));        strcpy(mat[i], tmp);        i++;    }    for(i=0;i<n-1;i++){        for(j=0;j<n-i-1;j++){            if(mat[j][0]>mat[j+1][0]){                tmp=mat[j];                mat[j]=mat[j+1];                mat[j+1]=tmp;            }        }    }    f2=fopen("output1.txt","w");    for(i=0;i<n;i++){        fprintf(f2,"%s\n",mat[i]);    }    return 0;}
